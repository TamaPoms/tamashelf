flask>=3.0
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.0

# Optionnel, seulement si tu actives le secours SAM (voir README.md) :
#   pip install torch --index-url https://download.pytorch.org/whl/cuXXX
#   pip install sam2 huggingface_hub          # pour SCAN_CLEANER_SAM=sam2
#   pip install segment-anything               # pour SCAN_CLEANER_SAM=sam1
